---
phase: 03-section-specific-category-page-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wordpress/wp-content/themes/charity-hcm/template-parts/content-song.php
  - wordpress/wp-content/themes/charity-hcm/template-parts/content-profile.php
  - wordpress/wp-content/themes/charity-hcm/template-parts/content-tip.php
autonomous: true
requirements:
  - SEC-03
  - SEC-04
  - SEC-05

must_haves:
  truths:
    - "A song card template part exists that renders a musical-note icon, song title link, and excerpt"
    - "A profile card template part exists that renders a post thumbnail, member name, and summary excerpt"
    - "A tip card template part exists that renders a compact card with category label, title, and short excerpt"
    - "All three template parts escape all output and include ABSPATH guard"
  artifacts:
    - path: "wordpress/wp-content/themes/charity-hcm/template-parts/content-song.php"
      provides: "Song card markup for tong-hop-bai-hat loop"
      contains: "song-card"
    - path: "wordpress/wp-content/themes/charity-hcm/template-parts/content-profile.php"
      provides: "Profile card markup for guong-mat-vuon-len loop"
      contains: "profile-card"
    - path: "wordpress/wp-content/themes/charity-hcm/template-parts/content-tip.php"
      provides: "Tip card markup for bi-kip and the-gioi-quanh-ta loops"
      contains: "tip-card"
  key_links:
    - from: "category.php (plan 03-02)"
      to: "template-parts/content-song.php"
      via: "get_template_part('template-parts/content', 'song')"
      pattern: "content-song"
    - from: "category.php (plan 03-02)"
      to: "template-parts/content-profile.php"
      via: "get_template_part('template-parts/content', 'profile')"
      pattern: "content-profile"
    - from: "category.php (plan 03-02)"
      to: "template-parts/content-tip.php"
      via: "get_template_part('template-parts/content', 'tip')"
      pattern: "content-tip"
---

<objective>
Create three new WordPress template parts that provide section-specific post card layouts for the Song Library, Profile showcase, and Tips sections.

Purpose: Plan 03-02 will wire these into `category.php` via `get_template_part()` dispatches. This plan creates the template files first so plan 03-02 can reference them.

Output: Three new PHP files in `template-parts/` following the same security and bilingual conventions as `content-card.php`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key patterns extracted from existing codebase — use these directly -->

From template-parts/content-card.php (reference pattern):
```php
<?php defined( 'ABSPATH' ) || exit; // implied by WP load, but guard at top if standalone ?>
// story-card pattern: get_the_ID(), post_class(), has_post_thumbnail(), the_post_thumbnail(),
// the_permalink(), the_title(), wp_trim_words(get_the_excerpt(), 30), get_the_category(),
// get_category_link($cats[0]->term_id), get_the_author_meta('ID'), get_the_date()
```

From functions.php (bilingual helper):
```php
charity_t( $vi_string, $en_string )
// Example: charity_t( 'Đọc tiếp', 'Read story' )
```

From functions.php (security conventions):
```php
esc_html( $string )   // all text output
esc_url( $url )       // all URL output
esc_attr( $attr )     // all attribute output
get_the_ID()          // numeric — safe
```

BEM naming used in this theme: .block, .block__element, .block--modifier
Content-pillar prefix: cp-* (for components in front-page). Template parts use descriptive block names.
Existing template part: content-card.php → produces .story-card elements.
New template parts should follow the same pattern: content-{type}.php → produces .{type}-card elements.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create song card and profile card template parts</name>
  <files>
    wordpress/wp-content/themes/charity-hcm/template-parts/content-song.php
    wordpress/wp-content/themes/charity-hcm/template-parts/content-profile.php
  </files>
  <read_first>
    - wordpress/wp-content/themes/charity-hcm/template-parts/content-card.php (reference: security guards, bilingual pattern, BEM class names)
    - wordpress/wp-content/themes/charity-hcm/functions.php (lines around charity_t function and ABSPATH)
  </read_first>
  <action>
Create TWO new files:

**File 1: `template-parts/content-song.php`**

```php
<?php
/**
 * Template Part: Song Card
 * Used by category.php for the tong-hop-bai-hat category page.
 */
defined( 'ABSPATH' ) || exit;
?>
<article id="post-<?php the_ID(); ?>" <?php post_class( 'song-card' ); ?>>
    <div class="song-card__inner">
        <div class="song-card__icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" xmlns="http://www.w3.org/2000/svg"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>
        </div>
        <div class="song-card__body">
            <h3 class="song-card__title">
                <a href="<?php the_permalink(); ?>"><?php the_title(); ?></a>
            </h3>
            <p class="song-card__excerpt"><?php echo esc_html( wp_trim_words( get_the_excerpt(), 20 ) ); ?></p>
            <a href="<?php the_permalink(); ?>" class="song-card__link">
                <?php echo charity_t( 'Xem bài hát', 'View Song' ); ?> &rarr;
            </a>
        </div>
    </div>
</article>
```

**File 2: `template-parts/content-profile.php`**

```php
<?php
/**
 * Template Part: Profile Card
 * Used by category.php for the guong-mat-vuon-len category page.
 */
defined( 'ABSPATH' ) || exit;
?>
<article id="post-<?php the_ID(); ?>" <?php post_class( 'profile-card' ); ?>>
    <div class="profile-card__inner">
        <?php if ( has_post_thumbnail() ) : ?>
        <div class="profile-card__photo">
            <a href="<?php the_permalink(); ?>">
                <?php the_post_thumbnail( 'thumbnail', [ 'alt' => esc_attr( get_the_title() ) ] ); ?>
            </a>
        </div>
        <?php endif; ?>
        <div class="profile-card__body">
            <h3 class="profile-card__name">
                <a href="<?php the_permalink(); ?>"><?php the_title(); ?></a>
            </h3>
            <p class="profile-card__summary"><?php echo esc_html( wp_trim_words( get_the_excerpt(), 25 ) ); ?></p>
            <a href="<?php the_permalink(); ?>" class="profile-card__link">
                <?php echo charity_t( 'Xem hồ sơ', 'View Profile' ); ?> &rarr;
            </a>
        </div>
    </div>
</article>
```

Both files: no raw `$_POST`/`$_GET`, all output escaped, ABSPATH guard at top.
  </action>
  <verify>
    <automated>grep -l "song-card" wordpress/wp-content/themes/charity-hcm/template-parts/content-song.php && grep -l "profile-card" wordpress/wp-content/themes/charity-hcm/template-parts/content-profile.php && grep "esc_html\|esc_url\|esc_attr" wordpress/wp-content/themes/charity-hcm/template-parts/content-song.php && grep "esc_html\|esc_url\|esc_attr" wordpress/wp-content/themes/charity-hcm/template-parts/content-profile.php</automated>
  </verify>
  <done>Both files exist. `content-song.php` contains `.song-card` block with musical note SVG, title link, excerpt, link. `content-profile.php` contains `.profile-card` with optional thumbnail, name, summary, link. Both use `esc_html()`, `esc_url()`, `esc_attr()`, and `charity_t()`. Both have `defined('ABSPATH') || exit`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tip card template part</name>
  <files>
    wordpress/wp-content/themes/charity-hcm/template-parts/content-tip.php
  </files>
  <read_first>
    - wordpress/wp-content/themes/charity-hcm/template-parts/content-card.php (category label pattern, BEM structure)
    - wordpress/wp-content/themes/charity-hcm/template-parts/content-song.php (just created — consistency reference)
  </read_first>
  <action>
Create `template-parts/content-tip.php`:

```php
<?php
/**
 * Template Part: Tip Card
 * Used by category.php for bi-kip and the-gioi-quanh-ta category pages.
 * Compact layout: category label, title, short excerpt, read-more link.
 */
defined( 'ABSPATH' ) || exit;

$post_cats = get_the_category();
?>
<article id="post-<?php the_ID(); ?>" <?php post_class( 'tip-card' ); ?>>
    <div class="tip-card__inner">
        <div class="tip-card__body">
            <?php if ( $post_cats ) : ?>
            <a href="<?php echo esc_url( get_category_link( $post_cats[0]->term_id ) ); ?>" class="tip-card__cat">
                <?php echo esc_html( $post_cats[0]->name ); ?>
            </a>
            <?php endif; ?>
            <h3 class="tip-card__title">
                <a href="<?php the_permalink(); ?>"><?php the_title(); ?></a>
            </h3>
            <p class="tip-card__excerpt"><?php echo esc_html( wp_trim_words( get_the_excerpt(), 18 ) ); ?></p>
            <a href="<?php the_permalink(); ?>" class="tip-card__read-more">
                <?php echo charity_t( 'Đọc thêm', 'Read more' ); ?> &rarr;
            </a>
        </div>
    </div>
</article>
```
  </action>
  <verify>
    <automated>grep -l "tip-card" wordpress/wp-content/themes/charity-hcm/template-parts/content-tip.php && grep "esc_html\|esc_url" wordpress/wp-content/themes/charity-hcm/template-parts/content-tip.php && grep "ABSPATH" wordpress/wp-content/themes/charity-hcm/template-parts/content-tip.php</automated>
  </verify>
  <done>`content-tip.php` exists. Contains `.tip-card` block with `.tip-card__cat`, `.tip-card__title`, `.tip-card__excerpt`, `.tip-card__read-more`. Uses `esc_html()`, `esc_url()`, `charity_t()`, ABSPATH guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WP loop → template output | WordPress post data rendered to HTML — must be escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information | content-profile.php | accept | Read-only display of published post data — no PII beyond author's public profile |
| T-03-02 | Tampering | XSS via unescaped output | mitigate | All dynamic output uses `esc_html()`, `esc_url()`, `esc_attr()` per CONVENTIONS.md |
| T-03-03 | Spoofing | ABSPATH bypass | mitigate | `defined('ABSPATH') \|\| exit` at top of each template part prevents direct file access |
</threat_model>

<verification>
- All three template parts exist in `template-parts/`
- Each file has `defined('ABSPATH') || exit`
- Each file uses `esc_html()`, `esc_url()`, `esc_attr()` for all dynamic output
- `charity_t()` used for all user-visible strings
- BEM class names consistent: `{type}-card`, `{type}-card__inner`, `{type}-card__body`, `{type}-card__title`
</verification>

<success_criteria>
Three new template parts created:
- `content-song.php` → song-card with musical note icon
- `content-profile.php` → profile-card with optional thumbnail + achievement summary
- `content-tip.php` → compact tip-card with category label
All output escaped. All files are WP-safe (ABSPATH guard).
</success_criteria>

<output>
After completion, create `.planning/phases/03-section-specific-category-page-templates/03-01-SUMMARY.md`
</output>
