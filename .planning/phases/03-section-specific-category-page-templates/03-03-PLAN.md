---
phase: 03-section-specific-category-page-templates
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified: []
autonomous: false
requirements:
  - SECT-06
  - SEC-03
  - SEC-04
  - SEC-05

must_haves:
  truths:
    - "All four category page layouts render correctly in a browser"
    - "All layouts are responsive on mobile"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification of all four section-specific category page layouts.

Purpose: Automated checks can confirm files exist and patterns are present, but cannot confirm the layouts look correct in a browser. This checkpoint gets human eyes on all four pages before marking Phase 3 complete.

Output: Human approval that all four layouts render correctly, or a list of issues to feed into gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 03-01 created three new template parts:
    - `template-parts/content-song.php` — song card with musical note icon
    - `template-parts/content-profile.php` — profile card with thumbnail + summary
    - `template-parts/content-tip.php` — compact tip card with category label

    Plan 03-02 wired them into `category.php` and added all CSS to `main.css`:
    - `tong-hop-bai-hat` → song-grid with song-cards
    - `guong-mat-vuon-len` → profile-grid with profile-cards
    - `bi-kip` + `the-gioi-quanh-ta` → tips-grid with tip-cards
    - All parent category pages → group icon in page-banner (SECT-06)
  </what-built>
  <how-to-verify>
    Visit these pages on your local WordPress install:

    1. **Song Library** (SEC-03):
       - URL: `http://localhost/category/tong-hop-bai-hat/`
       - ✓ Page shows a grid of song cards (NOT the generic post list)
       - ✓ Each song card has a musical note icon, title, and excerpt
       - ✓ Section header says "Kho lưu trữ bài hát / Song Library"
       - ✓ (if no posts: empty state message shown)

    2. **Profile Showcase** (SEC-05):
       - URL: `http://localhost/category/guong-mat-vuon-len/`
       - ✓ Page shows a grid of profile cards
       - ✓ Cards with featured images show the photo (no broken img if none)
       - ✓ Cards show member name and summary excerpt
       - ✓ Section header says "Những người Vươn Lên / Rise Up Members"

    3. **Tips Grid — Bí Kíp** (SEC-04):
       - URL: `http://localhost/category/bi-kip/`
       - ✓ Page shows compact tip cards (compact, left-accented with red border)
       - ✓ Each card has category label, title, short excerpt

    4. **Tips Grid — Thế giới quanh ta** (SEC-04):
       - URL: `http://localhost/category/the-gioi-quanh-ta/`
       - ✓ Same compact tip card grid as bi-kip

    5. **Rich Header with Icon** (SECT-06):
       - Visit any parent group category (e.g., `http://localhost/category/tin-tuc/`)
       - ✓ Page banner shows the group SVG icon above the title

    6. **Mobile** (responsive):
       - Resize browser to ~400px wide
       - ✓ All grids collapse to single column
       - ✓ No horizontal overflow

    7. **Other categories unaffected**:
       - Visit `http://localhost/category/chuyen-vuon-len/`
       - ✓ Still shows the generic post list (unchanged)
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 3 complete, or describe any visual issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| — | This is a verification-only plan. No code execution or data processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | — | Verification plan | accept | No attack surface — human review only, no code changes |
</threat_model>

<verification>
Human confirms all four layouts render correctly in browser.
Phase 3 success criteria verified:
1. `tong-hop-bai-hat` shows song card grid ✓
2. `guong-mat-vuon-len` shows profile card grid with photo and achievement summary ✓
3. `bi-kip` and `the-gioi-quanh-ta` show compact tips card grid ✓
4. All new layouts are responsive on mobile ✓
5. All parent category pages show group icon in page-banner (SECT-06) ✓
</verification>

<success_criteria>
Human types "approved" confirming all four section-specific category page layouts render correctly and are responsive on mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/03-section-specific-category-page-templates/03-03-SUMMARY.md`
</output>
