---
phase: 05-readme-documentation-update
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02

must_haves:
  truths:
    - "README.md accurately lists all 5 sections with names, slugs, and subsections"
    - "Screenshots references use the correct .png extension matching actual files"
    - "Highlights section mentions Phase 4 social links, interactive map, and current v2.2.0 theme"
    - "Quick-start instructions reference correct file paths and setup steps"
  artifacts:
    - path: "README.md"
      provides: "Updated documentation with accurate content architecture"
  key_links:
    - from: "README.md"
      to: "docs/screenshots/localhost-home.png"
      via: "markdown image link"
      pattern: "localhost-home\\.png"
---

<objective>
Update README.md to accurately reflect the current state of the project after Phases 1–4 and 6 completion.

Purpose: README currently references non-existent `.jpg` screenshots (actual files are `.png`), is missing the interactive map feature, and doesn't mention Phase 4 navigation/social improvements. Documentation must be accurate.
Output: An updated README.md with correct screenshot extensions, updated Highlights, and accurate Quick Start.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix screenshot references and update Highlights in README.md</name>
  <files>README.md</files>
  <action>
    1. In "Localhost Preview" section: change all `docs/screenshots/localhost-*.jpg` to `.png` — the actual files are `localhost-home.png` and `localhost-announcements.png` (no contact screenshot exists yet, so remove or comment that block).
    2. Update the Highlights bullet list to include:
       - "Interactive Vietnam student origin map with 34-province / 63-province toggle (Phase 6)"
       - "Social links wired via Customizer (Facebook, YouTube, TikTok) with Open Graph meta tags (Phase 4)"
       Remove any stale/placeholder bullets that no longer apply.
    3. Verify the 5 Content Sections table is complete and matches the slugs in `Y_Tuong_De_Muc_Website_HBVL.md`:
       - tin-tuc, dong-du-ky, so-tay-kien-thuc, goc-sach-hay, sinh-hoat (no changes expected, just verify).
    4. In Quick Start section: verify the PHP server command matches `php -S 127.0.0.1:8080 -t wordpress` and fix if wrong.
  </action>
  <verify>grep -n "localhost-home.png\|localhost-announcements.png" README.md && ! grep -q "localhost.*\.jpg" README.md && echo "PASS: screenshot refs correct"</verify>
  <done>README.md has .png screenshot refs, updated Highlights bullet list, and verified Quick Start command.</done>
</task>

<task type="auto">
  <name>Task 2: Update Quick Start instructions and theme version reference</name>
  <files>README.md</files>
  <action>
    1. Confirm the theme version badge / mention reads `v2.2.0` (it should already — just verify).
    2. In Quick Start / Local Setup section: ensure the steps include:
       - "Clone repo and navigate to project root"
       - "`php -S 127.0.0.1:8080 -t wordpress`" as the server start command
       - "Visit http://127.0.0.1:8080 in browser"
       Fix any incorrect port numbers or paths.
    3. Add a "What's New" or "Recent Updates" callout note (using GitHub Note syntax `> [!NOTE]`) briefly listing completed work:
       - Phase 2: Homepage section cards with icons and color accents
       - Phase 4: Navigation menu, social links, Open Graph SEO
       - Phase 6: Contact address updated, interactive student map with province toggle
  </action>
  <verify>grep -n "8080\|What's New\|Recent Updates\|\[!NOTE\]" README.md | head -20</verify>
  <done>Quick Start has correct server command, theme version is v2.2.0, and a recent updates note exists.</done>
</task>

</tasks>

<verification>
- README.md has no references to `.jpg` screenshots
- README.md references `localhost-home.png` and `localhost-announcements.png`
- Highlights section includes interactive map and social links
- Quick Start uses port 8080
- Theme version shows v2.2.0
</verification>

<success_criteria>
1. `grep "\.jpg" README.md` returns zero results in the screenshots section
2. `grep "localhost-home\.png" README.md` returns a match
3. README Highlights mentions "interactive" map and "social links"
4. Quick Start section has `8080`
</success_criteria>

<output>
After completion, create `.planning/phases/05-readme-documentation-update/05-01-SUMMARY.md`
</output>
