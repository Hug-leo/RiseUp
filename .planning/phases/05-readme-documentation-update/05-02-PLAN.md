---
phase: 05-readme-documentation-update
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - docs/screenshots/localhost-home.png
  - docs/screenshots/localhost-contact.png
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "docs/screenshots/ contains up-to-date PNG screenshots showing the current UI"
    - "README.md screenshot references resolve to existing files"
  artifacts:
    - path: "docs/screenshots/localhost-home.png"
      provides: "Homepage screenshot (may already be current)"
    - path: "docs/screenshots/localhost-contact.png"
      provides: "Contact page screenshot (new)"
  key_links:
    - from: "README.md"
      to: "docs/screenshots/"
      via: "markdown image links"
      pattern: "docs/screenshots/"
---

<objective>
Verify existing screenshots still accurately represent the site after UI changes, and add a missing contact page screenshot.

Purpose: The README references a contact screenshot that doesn't exist (`localhost-contact.jpg` was in README — now `.png`). We need to ensure the screenshot set is complete and files referenced in README exist.
Output: Complete and consistent screenshot set in `docs/screenshots/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing screenshots vs README references</name>
  <files>docs/screenshots/</files>
  <action>
    1. Run `ls docs/screenshots/` to inventory all existing screenshot files.
    2. Cross-reference against all `docs/screenshots/` image references in README.md.
    3. For any reference in README that has no matching file: either (a) add a placeholder note that the screenshot will be captured manually, or (b) remove the broken reference from README — prefer (b) to keep README accurate. Do NOT leave broken image references.
    4. If `localhost-contact.png` does not exist and README references it, remove that image block from README.md entirely (with a comment: "screenshot to be added after first production deployment").
  </action>
  <verify>
    IMGS=$(grep -oP '(?&lt;=!\[)[^\]]*\]\(docs/screenshots/[^)]+\)' README.md | grep -oP 'docs/screenshots/[^)]+'); for f in $IMGS; do [ -f "/Users/phatcnguyen/Downloads/RiseUp/$f" ] || echo "MISSING: $f"; done; echo "audit complete"
  </verify>
  <done>Every screenshot image referenced in README.md has a corresponding file in docs/screenshots/. No broken image links.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>README.md is fully updated (Phase 5 plans 01 + 02 complete). Screenshot references are valid, Highlights are current, Quick Start is accurate.</what-built>
  <how-to-verify>
    1. Open README.md in a Markdown viewer or GitHub preview
    2. Confirm "Localhost Preview" images resolve (no broken image icons)
    3. Confirm Highlights includes: interactive map, social links, Open Graph
    4. Confirm Quick Start has: `php -S 127.0.0.1:8080 -t wordpress`
    5. Confirm no `.jpg` references remain for screenshots
  </how-to-verify>
  <resume-signal>Type "approved" when verified, or describe what needs fixing</resume-signal>
</task>

</tasks>

<verification>
- `ls docs/screenshots/` lists files matching all README image references
- No `![...](.../screenshots/*.jpg)` in README
- README renders cleanly with no broken images
</verification>

<success_criteria>
1. Every `docs/screenshots/*.png` referenced in README exists on disk
2. README.md has no `.jpg` screenshot references
3. Phase 5 DOC-01 and DOC-02 requirements are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-readme-documentation-update/05-02-SUMMARY.md`
</output>
