---
phase: 02-section-cards-ui-homepage-visual-polish
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - wordpress/wp-content/themes/charity-hcm/assets/css/main.css
autonomous: true
requirements: [HOME-05, HOME-06, UI-05]

must_haves:
  truths:
    - .cp-group-card:nth-child(n) rules with animation-delay exist (stagger effect)
    - .cp-group-card hover lift is at least translateY(-4px)
    - .cp-group-card transition is smooth (cubic-bezier or ease-out)
    - .cp-group-card__icon svg has a transition on color/opacity
  artifacts:
    - wordpress/wp-content/themes/charity-hcm/assets/css/main.css
  key_links: []
---

<objective>
Polish card hover animations and add staggered animate-in delays to the section cards.

Purpose: Cards already have basic hover (translateY -3px) and animate-in from Phase 1. This plan enhances the feel: deeper hover lift, smooth cubic-bezier easing, and per-card stagger delays so the 5 cards cascade into view rather than all popping in simultaneously.
Output: main.css gets stagger nth-child delays and improved hover transitions on cp-group-card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@wordpress/wp-content/themes/charity-hcm/assets/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve cp-group-card hover and add stagger animation delays</name>
  <files>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</files>
  <read_first>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</read_first>
  <action>
    1. Find the existing `.cp-group-card` base rule (around line 1964). Update its `transition`
       property to use a cubic-bezier for a snappier feel:
       transition: transform .25s cubic-bezier(.4,0,.2,1), box-shadow .25s cubic-bezier(.4,0,.2,1);

    2. Find `.cp-group-card:hover`. Update transform to translateY(-5px) for a more noticeable lift:
       transform: translateY(-5px);
       box-shadow: 0 16px 40px rgba(14, 68, 146, 0.18);

    3. After the per-group accent color rules (from plan 02-01), add stagger delays so cards
       inside the grid animate in sequentially. Use nth-child on the article:
       
       .cp-category-grid .cp-group-card.animate-in:nth-child(1) { transition-delay: 0s; }
       .cp-category-grid .cp-group-card.animate-in:nth-child(2) { transition-delay: .08s; }
       .cp-category-grid .cp-group-card.animate-in:nth-child(3) { transition-delay: .16s; }
       .cp-category-grid .cp-group-card.animate-in:nth-child(4) { transition-delay: .24s; }
       .cp-category-grid .cp-group-card.animate-in:nth-child(5) { transition-delay: .32s; }

    Do NOT remove or duplicate any existing rules. Only modify the transition/transform values
    in-place and append the stagger rules after the accent color block.
  </action>
  <verify>grep -c "transition-delay" wordpress/wp-content/themes/charity-hcm/assets/css/main.css</verify>
  <acceptance_criteria>
    - transition property on .cp-group-card uses cubic-bezier
    - hover transform is translateY(-5px)
    - 5 transition-delay rules exist for nth-child(1) through nth-child(5)
  </acceptance_criteria>
  <done>grep -c "transition-delay" returns 5</done>
</task>

</tasks>
