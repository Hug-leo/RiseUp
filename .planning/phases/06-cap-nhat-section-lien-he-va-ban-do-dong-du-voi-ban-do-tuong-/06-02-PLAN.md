---
phase: 06-contact-and-student-map
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-63-provinces.svg
  - wordpress/wp-content/themes/charity-hcm/assets/js/student-map.js
  - wordpress/wp-content/themes/charity-hcm/assets/css/main.css
  - wordpress/wp-content/themes/charity-hcm/category.php
  - wordpress/wp-content/themes/charity-hcm/functions.php
autonomous: true
requirements:
  - MAP-01
  - MAP-02

must_haves:
  truths:
    - "ban-do-vuon-len category page shows an interactive SVG choropleth map, not a static image"
    - "Provinces with sample student data are visually highlighted with a fill color"
    - "Hovering a highlighted province shows a tooltip with province name and student count"
    - "student-map.js loads only on the ban-do-vuon-len category page"
  artifacts:
    - path: "wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-63-provinces.svg"
      provides: "Self-hosted SVG map with province paths using ISO 3166-2:VN IDs"
      contains: "viewBox"
    - path: "wordpress/wp-content/themes/charity-hcm/assets/js/student-map.js"
      provides: "Choropleth coloring + hover tooltip logic"
      exports: ["IIFE — no globals except window.vuonlenMap consumed"]
    - path: "wordpress/wp-content/themes/charity-hcm/category.php"
      provides: "Replaced category-map-demo section with student-map-section"
      contains: "student-map-section"
  key_links:
    - from: "functions.php wp_localize_script"
      to: "student-map.js window.vuonlenMap"
      via: "wp_localize_script('charity-student-map', 'vuonlenMap', [...])"
      pattern: "vuonlenMap"
    - from: "student-map.js"
      to: "vietnam-63-provinces.svg paths"
      via: "document.querySelector('#student-map-63 path[id=\"' + code + '\"]')"
      pattern: "querySelector.*path"
---

<objective>
Build the interactive 63-province student origin map: self-host the SVG, create the choropleth JS with sample data, add CSS styling, and replace the static map demo in category.php.

Purpose: The ban-do-vuon-len category page currently shows a static image with 3 animated pins and a placeholder note "A later phase can replace this mockup." This IS that later phase.
Output: Inline interactive SVG choropleth with province fill + hover tooltip, loaded conditionally via wp_enqueue_script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cap-nhat-section-lien-he-va-ban-do-dong-du-voi-ban-do-tuong-/06-RESEARCH.md
</context>

<interfaces>
<!-- Existing category.php section to REPLACE (lines ~90-112) -->
<!-- The ENTIRE block below is replaced by the new student-map-section: -->
```php
<?php if ( $current_item && $current_item['slug'] === 'ban-do-vuon-len' ) : ?>
    <section class="category-map-demo">
        <div class="category-map-demo__map" aria-hidden="true">
            <img src="..." alt="">
            <span class="cp-map-pin cp-map-pin--north"></span>
            ...
        </div>
        <div class="category-map-demo__content">...</div>
    </section>
<?php endif; ?>
```

<!-- functions.php enqueue pattern (lines ~34-67) -->
```php
add_action( 'wp_enqueue_scripts', function () {
    wp_enqueue_style( 'charity-hcm-main', CHARITY_HCM_URI . '/assets/css/main.css', [], CHARITY_HCM_VERSION );
    wp_enqueue_script( 'charity-hcm-main', CHARITY_HCM_URI . '/assets/js/main.js', [], CHARITY_HCM_VERSION, true );
    wp_localize_script( 'charity-hcm-main', 'charityHCM', [...] );
    // ADD: conditional student-map enqueue below existing localize_script call
} );
```

<!-- charity_t() helper signature -->
charity_t( string $vi, string $en ): string
<!-- charity_get_lang() — returns 'vi' or 'en', used to pass active lang to JS -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create self-hosted Vietnam 63-province SVG and update category.php</name>
  <files>
    wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-63-provinces.svg
    wordpress/wp-content/themes/charity-hcm/category.php
  </files>
  <action>
    **Step A: Create vietnam-63-provinces.svg**

    Create a simplified Vietnam 63-province SVG at `assets/img/vietnam-63-provinces.svg`.
    Requirements for the SVG:
    - Must have `viewBox="0 0 400 800"` (or similar aspect-ratio-correct value for Vietnam's shape)
    - Must NOT have fixed `width` or `height` attributes (use CSS for sizing)
    - Each province `<path>` must have `id` matching ISO 3166-2:VN codes:
      - Hanoi: `VN-HN`, Ho Chi Minh City: `VN-SG`, Da Nang: `VN-DN`, Can Tho: `VN-CT`
      - Hai Phong: `VN-HP`, Hue (TT-Hue): `VN-TH`, Nghe An: `VN-NA`, Thanh Hoa: `VN-TH` wait — use the correct 2-letter codes for each province
    - Use these ISO 3166-2:VN province codes for the `id` attributes:
      VN-44 (An Giang), VN-43 (Bà Rịa–Vũng Tàu), VN-54 (Bắc Giang), VN-53 (Bắc Kạn),
      VN-55 (Bạc Liêu), VN-56 (Bắc Ninh), VN-50 (Bến Tre), VN-31 (Bình Định),
      VN-57 (Bình Dương), VN-58 (Bình Phước), VN-40 (Bình Thuận), VN-59 (Cà Mau),
      VN-CT (Cần Thơ), VN-04 (Cao Bằng), VN-DN (Đà Nẵng), VN-33 (Đắk Lắk),
      VN-72 (Đắk Nông), VN-71 (Điện Biên), VN-39 (Đồng Nai), VN-45 (Đồng Tháp),
      VN-30 (Gia Lai), VN-03 (Hà Giang), VN-63 (Hà Nam), VN-HN (Hà Nội),
      VN-23 (Hà Tĩnh), VN-61 (Hải Dương), VN-HP (Hải Phòng), VN-73 (Hậu Giang),
      VN-14 (Hòa Bình), VN-66 (Hưng Yên), VN-34 (Khánh Hòa), VN-47 (Kiên Giang),
      VN-28 (Kon Tum), VN-01 (Lai Châu), VN-35 (Lâm Đồng), VN-09 (Lạng Sơn),
      VN-02 (Lào Cai), VN-41 (Long An), VN-67 (Nam Định), VN-22 (Nghệ An),
      VN-18 (Ninh Bình), VN-36 (Ninh Thuận), VN-68 (Phú Thọ), VN-32 (Phú Yên),
      VN-24 (Quảng Bình), VN-27 (Quảng Nam), VN-29 (Quảng Ngãi), VN-13 (Quảng Ninh),
      VN-25 (Quảng Trị), VN-52 (Sóc Trăng), VN-05 (Sơn La), VN-37 (Tây Ninh),
      VN-20 (Thái Bình), VN-69 (Thái Nguyên), VN-21 (Thanh Hóa), VN-26 (Thừa Thiên Huế),
      VN-46 (Tiền Giang), VN-SG (TP. Hồ Chí Minh), VN-51 (Trà Vinh), VN-07 (Tuyên Quang),
      VN-49 (Vĩnh Long), VN-70 (Vĩnh Phúc), VN-06 (Yên Bái)
    - Paths should be stylistically clean: `fill="var(--province-fill, #e8ead6)"` `stroke="#fff"` `stroke-width="0.5"`
    - Wrap all paths in `<g id="provinces-63">` for JS targeting
    - Include a `<title>Bản đồ 63 tỉnh thành Việt Nam</title>` as first child

    Note: Generate a SIMPLIFIED but geographically plausible SVG. The shapes don't need millimeter accuracy — they need to be recognizable as Vietnam's provinces. Use a simplified path approach with correct relative positions (north → south, coastal provinces on the right/east, mountainous provinces on left/west).

    **Step B: Replace category-map-demo in category.php**

    Find the existing `ban-do-vuon-len` conditional block in `category.php` and REPLACE the entire `<section class="category-map-demo">...</section>` with:

    ```php
    <section class="student-map-section" id="student-map-interactive">
        <div class="container">
            <div class="student-map__header">
                <h2 class="student-map__title">
                    <?php echo charity_t( 'Bản đồ sinh viên Vươn Lên', 'Rise Up Student Origins' ); ?>
                </h2>
                <p class="student-map__desc">
                    <?php echo charity_t(
                        'Nơi xuất phát của các thành viên và cựu học sinh chương trình Học Bổng Vươn Lên trên khắp Việt Nam.',
                        'Origins of Rise Up Scholarship members and alumni across Vietnam.'
                    ); ?>
                </p>
            </div>

            <div class="student-map__wrap">
                <!-- SVG container: 63-province view (default active) -->
                <div class="student-map__svg-container active" id="student-map-63" aria-hidden="true">
                    <?php
                    $svg_63 = get_template_directory() . '/assets/img/vietnam-63-provinces.svg';
                    if ( file_exists( $svg_63 ) ) {
                        // phpcs:ignore WordPress.WP.AlternativeFunctions.file_get_contents_file_get_contents
                        echo file_get_contents( $svg_63 ); // Inline SVG for JS manipulation
                    }
                    ?>
                </div>

                <!-- Tooltip (shared across both maps) -->
                <div class="student-map__tooltip" id="student-map-tooltip" role="tooltip" aria-live="polite"></div>
            </div>

            <p class="student-map__note">
                <?php echo charity_t(
                    '* Dữ liệu minh họa — số lượng thực tế sẽ được cập nhật trong các phiên bản tới.',
                    '* Sample data — actual numbers will be updated in future versions.'
                ); ?>
            </p>
        </div>
    </section>
    ```

    The `file_get_contents()` approach inlines the SVG directly into the DOM, making all `<path>` elements queryable by `student-map.js`.
  </action>
  <verify>
    <automated>
      test -f wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-63-provinces.svg &amp;&amp; echo "SVG exists" &amp;&amp; grep -c "&lt;path" wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-63-provinces.svg &amp;&amp; grep "student-map-section" wordpress/wp-content/themes/charity-hcm/category.php
    </automated>
  </verify>
  <done>
    SVG file exists with 60+ `&lt;path` elements. `category.php` contains "student-map-section". The old "category-map-demo" section is gone from the ban-do-vuon-len conditional block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create student-map.js choropleth logic and add CSS to main.css + conditional enqueue in functions.php</name>
  <files>
    wordpress/wp-content/themes/charity-hcm/assets/js/student-map.js
    wordpress/wp-content/themes/charity-hcm/assets/css/main.css
    wordpress/wp-content/themes/charity-hcm/functions.php
  </files>
  <action>
    **Step A: Create assets/js/student-map.js**

    ```js
    /* Rise Up Scholarship — student-map.js
     * Interactive choropleth for Bản đồ Vươn Lên (student origin map).
     * Requires: window.vuonlenMap (set via wp_localize_script)
     * Reads:    #student-map-63 SVG paths by province ISO code
     */
    (function () {
      'use strict';

      document.addEventListener('DOMContentLoaded', function () {
        var mapData = window.vuonlenMap;
        if (!mapData || !mapData.students) { return; }

        var tooltip = document.getElementById('student-map-tooltip');
        var activeContainer = document.getElementById('student-map-63');

        // Apply fill classes to provinces with student data
        function applyData(container, students) {
          if (!container) { return; }
          // Reset all province fills first
          container.querySelectorAll('path').forEach(function (path) {
            path.classList.remove('province--low', 'province--mid', 'province--high', 'province--active');
          });
          students.forEach(function (entry) {
            var path = container.querySelector('#' + entry.code);
            if (!path) { return; }
            path.dataset.labelVi = entry.label_vi;
            path.dataset.labelEn = entry.label_en;
            path.dataset.count   = entry.count;
            path.classList.add('province--active');
            if (entry.count >= 8) {
              path.classList.add('province--high');
            } else if (entry.count >= 4) {
              path.classList.add('province--mid');
            } else {
              path.classList.add('province--low');
            }
          });
        }

        // Tooltip show/hide
        function showTooltip(e, path) {
          if (!tooltip) { return; }
          var lang    = mapData.lang || 'vi';
          var label   = lang === 'en' ? (path.dataset.labelEn || path.dataset.labelVi) : path.dataset.labelVi;
          var count   = path.dataset.count;
          var unit    = lang === 'en' ? ' students' : ' sinh viên';
          tooltip.textContent = label + ': ' + count + unit;
          tooltip.classList.add('visible');
          positionTooltip(e);
        }

        function hideTooltip() {
          if (tooltip) { tooltip.classList.remove('visible'); }
        }

        function positionTooltip(e) {
          if (!tooltip) { return; }
          var wrap  = document.querySelector('.student-map__wrap');
          var rect  = wrap ? wrap.getBoundingClientRect() : { left: 0, top: 0 };
          var x     = (e.clientX || (e.touches && e.touches[0].clientX) || 0) - rect.left + 12;
          var y     = (e.clientY || (e.touches && e.touches[0].clientY) || 0) - rect.top  - 36;
          tooltip.style.left = x + 'px';
          tooltip.style.top  = y + 'px';
        }

        // Bind hover events to active container
        function bindEvents(container) {
          if (!container) { return; }
          container.querySelectorAll('path.province--active').forEach(function (path) {
            path.addEventListener('mouseenter', function (e) { showTooltip(e, path); });
            path.addEventListener('mousemove',  function (e) { positionTooltip(e); });
            path.addEventListener('mouseleave', hideTooltip);
            path.addEventListener('touchstart', function (e) {
              e.preventDefault();
              showTooltip(e, path);
            }, { passive: false });
            path.addEventListener('touchend', function () {
              setTimeout(hideTooltip, 1800);
            });
          });
        }

        // Initial render
        applyData(activeContainer, mapData.students);
        bindEvents(activeContainer);
      });
    }());
    ```

    **Step B: Append CSS section to main.css**

    Append the following block at the END of `main.css` (after all existing rules):

    ```css
    /* ── Student Map (Phase 6) ────────────────────────────────────────────── */
    .student-map-section {
      padding: var(--space-xl) 0;
      background: var(--color-bg-light, #f8f7f3);
    }

    .student-map__header {
      text-align: center;
      margin-bottom: var(--space-lg);
    }

    .student-map__title {
      font-size: clamp(1.4rem, 3vw, 2rem);
      font-weight: 700;
      color: var(--color-heading, #1a1a1a);
      margin-bottom: 0.5rem;
    }

    .student-map__desc {
      color: var(--color-text-muted, #666);
      max-width: 560px;
      margin: 0 auto;
    }

    .student-map__wrap {
      position: relative;
      max-width: 360px;
      margin: 0 auto var(--space-md);
    }

    .student-map__svg-container {
      display: none;
      width: 100%;
    }

    .student-map__svg-container.active {
      display: block;
    }

    .student-map__svg-container svg {
      width: 100%;
      height: auto;
      display: block;
    }

    /* Province fill tiers */
    .student-map__svg-container path {
      fill: var(--province-fill, #e8ead6);
      stroke: #fff;
      stroke-width: 0.5;
      transition: fill 0.2s ease;
      cursor: default;
    }

    .student-map__svg-container path.province--active {
      cursor: pointer;
    }

    .student-map__svg-container path.province--low {
      fill: #f4c4b2;
    }

    .student-map__svg-container path.province--mid {
      fill: #e07a5f;
    }

    .student-map__svg-container path.province--high {
      fill: #c0392b;
    }

    .student-map__svg-container path.province--active:hover,
    .student-map__svg-container path.province--active:focus {
      fill: #922b21;
      outline: none;
    }

    /* Tooltip */
    .student-map__tooltip {
      position: absolute;
      background: rgba(26, 26, 26, 0.88);
      color: #fff;
      font-size: 0.8rem;
      padding: 5px 10px;
      border-radius: 4px;
      pointer-events: none;
      white-space: nowrap;
      opacity: 0;
      transition: opacity 0.15s ease;
      z-index: 10;
    }

    .student-map__tooltip.visible {
      opacity: 1;
    }

    .student-map__note {
      text-align: center;
      font-size: 0.75rem;
      color: var(--color-text-muted, #999);
      margin-top: var(--space-sm);
    }

    @media (max-width: 768px) {
      .student-map__wrap {
        max-width: 280px;
      }
    }
    /* ── /Student Map ────────────────────────────────────────────────────── */
    ```

    **Step C: Add conditional wp_enqueue_script in functions.php**

    Inside the existing `add_action( 'wp_enqueue_scripts', function () { ... } )` block, add these lines AFTER the existing `wp_localize_script( 'charity-hcm-main', 'charityHCM', [...] )` call:

    ```php
    // Student origin map — only on ban-do-vuon-len category page
    if ( is_category() ) {
        $queried = get_queried_object();
        if ( $queried instanceof WP_Term && $queried->slug === 'ban-do-vuon-len' ) {
            wp_enqueue_script(
                'charity-student-map',
                CHARITY_HCM_URI . '/assets/js/student-map.js',
                [],
                CHARITY_HCM_VERSION,
                true
            );
            $student_data = [
                [ 'code' => 'VN-SG', 'label_vi' => 'TP. Hồ Chí Minh', 'label_en' => 'Ho Chi Minh City', 'count' => 12 ],
                [ 'code' => 'VN-HN', 'label_vi' => 'Hà Nội',           'label_en' => 'Hanoi',            'count' => 8  ],
                [ 'code' => 'VN-DN', 'label_vi' => 'Đà Nẵng',          'label_en' => 'Da Nang',          'count' => 5  ],
                [ 'code' => 'VN-CT', 'label_vi' => 'Cần Thơ',          'label_en' => 'Can Tho',          'count' => 4  ],
                [ 'code' => 'VN-HP', 'label_vi' => 'Hải Phòng',        'label_en' => 'Hai Phong',        'count' => 3  ],
                [ 'code' => 'VN-26', 'label_vi' => 'Thừa Thiên Huế',   'label_en' => 'Thua Thien Hue',   'count' => 3  ],
                [ 'code' => 'VN-22', 'label_vi' => 'Nghệ An',          'label_en' => 'Nghe An',          'count' => 2  ],
                [ 'code' => 'VN-35', 'label_vi' => 'Lâm Đồng',        'label_en' => 'Lam Dong',         'count' => 2  ],
                [ 'code' => 'VN-21', 'label_vi' => 'Thanh Hóa',        'label_en' => 'Thanh Hoa',        'count' => 2  ],
                [ 'code' => 'VN-34', 'label_vi' => 'Khánh Hòa',        'label_en' => 'Khanh Hoa',        'count' => 1  ],
                [ 'code' => 'VN-40', 'label_vi' => 'Bình Thuận',       'label_en' => 'Binh Thuan',       'count' => 1  ],
                [ 'code' => 'VN-57', 'label_vi' => 'Bình Dương',       'label_en' => 'Binh Duong',       'count' => 1  ],
            ];
            wp_localize_script( 'charity-student-map', 'vuonlenMap', [
                'lang'     => function_exists( 'charity_get_lang' ) ? charity_get_lang() : 'vi',
                'students' => $student_data,
            ] );
        }
    }
    ```

    Note: `instanceof WP_Term` check prevents fatal error if `get_queried_object()` returns null or non-term object.
  </action>
  <verify>
    <automated>
      test -f wordpress/wp-content/themes/charity-hcm/assets/js/student-map.js &amp;&amp; echo "JS exists" &amp;&amp; grep "vuonlenMap" wordpress/wp-content/themes/charity-hcm/functions.php &amp;&amp; grep "student-map" wordpress/wp-content/themes/charity-hcm/assets/css/main.css | head -3
    </automated>
  </verify>
  <done>
    student-map.js exists. functions.php contains "vuonlenMap". main.css contains ".student-map-section". No PHP syntax errors (run `php -l functions.php` to confirm).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PHP → HTML output (file_get_contents) | SVG file read from theme directory and echoed inline |
| JS → DOM | student-map.js reads window.vuonlenMap (set by wp_localize_script) and manipulates SVG paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | file_get_contents(SVG) | mitigate | SVG path uses `get_template_directory()` (server-local, not user-controlled). Path is hardcoded, not from user input. No path traversal possible. |
| T-06-02-02 | XSS | Inline SVG output | mitigate | SVG is a static theme asset, not user-supplied. No `<script>` tags expected inside SVG. Add comment in code noting SVG must not include script elements. |
| T-06-02-03 | Information Disclosure | wp_localize_script data | accept | Student data is intentionally public sample data — no PII, no auth tokens. |
| T-06-02-04 | Spoofing | JS reading window.vuonlenMap | accept | Data comes from PHP-controlled wp_localize_script; JS does not make AJAX calls or trust external input. |
</threat_model>

<verification>
1. Visit http://127.0.0.1:8080/?cat={ban-do-vuon-len-id}
2. Confirm: static image + pins are GONE; interactive SVG map is present
3. Open DevTools → Elements: confirm SVG has `<path id="VN-SG">` etc.
4. Open DevTools → Console: confirm no JS errors
5. Hover over Ho Chi Minh City (VN-SG) province: tooltip shows "TP. Hồ Chí Minh: 12 sinh viên"
6. Confirm VN-SG is dark red (province--high), VN-HP is light red (province--low)
7. Open DevTools → Network: confirm student-map.js loads ONLY on this category, not on homepage or other pages
</verification>

<success_criteria>
Interactive SVG choropleth renders on ban-do-vuon-len category page. 12 sample provinces are visually highlighted by data tier. Hover tooltip shows province name + count. student-map.js does not load on other pages.
</success_criteria>

<output>
After completion, create `.planning/phases/06-cap-nhat-section-lien-he-va-ban-do-dong-du-voi-ban-do-tuong-/06-02-SUMMARY.md`
</output>
