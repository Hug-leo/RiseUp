---
phase: 06-contact-and-student-map
plan: 03
type: execute
wave: 2
depends_on:
  - 06-02
files_modified:
  - wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-34-provinces.svg
  - wordpress/wp-content/themes/charity-hcm/assets/js/student-map.js
  - wordpress/wp-content/themes/charity-hcm/assets/css/main.css
  - wordpress/wp-content/themes/charity-hcm/category.php
  - wordpress/wp-content/themes/charity-hcm/functions.php
autonomous: true
requirements:
  - MAP-03

must_haves:
  truths:
    - "Toggle buttons labeled '63 tỉnh thành' and '34 tỉnh thành' appear above the map"
    - "Clicking the 34-province button replaces the map with the new 2025 administrative SVG"
    - "Clicking the 63-province button restores the classic map"
    - "Map is usable and legible on a 375px wide mobile screen"
    - "Active toggle button has visible highlighted state"
  artifacts:
    - path: "wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-34-provinces.svg"
      provides: "Self-hosted SVG for Vietnam's new 34-province administrative map (2025)"
      contains: "viewBox"
    - path: "wordpress/wp-content/themes/charity-hcm/category.php"
      provides: "Second SVG container (#student-map-34) rendered alongside #student-map-63"
      contains: "student-map-34"
  key_links:
    - from: "Toggle button click"
      to: ".student-map__svg-container.active swap"
      via: "JS classList toggle on both containers"
      pattern: "classList.*active"
    - from: "functions.php vuonlenMap"
      to: "student-map.js students_34 array"
      via: "wp_localize_script vuonlenMap.students_34"
      pattern: "students_34"
---

<objective>
Add the 34-province toggle: self-host the new 2025 Vietnam administrative map SVG, wire toggle buttons in PHP, extend JS to swap views and re-apply choropleth, and add mobile-responsive CSS polish.

Purpose: Phase 6 success criteria require a toggle between the classic 63-province map and the new 2025 34-province map. Plan 02 built the 63-province base; this plan adds the second map and the toggle mechanism.
Output: Working toggle between two SVG views with choropleth data, polished for mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cap-nhat-section-lien-he-va-ban-do-dong-du-voi-ban-do-tuong-/06-RESEARCH.md
@.planning/phases/06-cap-nhat-section-lien-he-va-ban-do-dong-du-voi-ban-do-tuong-/06-02-SUMMARY.md
</context>

<interfaces>
<!-- After Plan 02, category.php student-map-section contains: -->
```html
<div class="student-map__wrap">
  <div class="student-map__svg-container active" id="student-map-63"><!-- SVG --></div>
  <div class="student-map__tooltip" id="student-map-tooltip"></div>
</div>
```

<!-- student-map.js from Plan 02 exports: applyData(), bindEvents() as internal functions -->
<!-- To add toggle, extend the IIFE — add toggle button event listeners that: -->
<!--   1. Set the clicked container as active (classList swap) -->
<!--   2. Call applyData() + bindEvents() on the newly active container -->

<!-- window.vuonlenMap from Plan 02 has: { lang, students } -->
<!-- Plan 03 adds: { students_34 } to the localize data -->

<!-- Vietnam 34-province codes (post-2025 merger): -->
<!-- The 34 new administrative units use new codes. Map using a consistent prefix: -->
<!-- VN34-HN (Hà Nội + merged), VN34-SG (TP.HCM + merged), VN34-HP (Hải Phòng), -->
<!-- VN34-DN (Đà Nẵng), VN34-CT (Cần Thơ), etc. — 34 total units. -->
<!-- Use VN34-XX prefix to distinguish from 63-province VN-XX codes. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create vietnam-34-provinces.svg and add toggle UI to category.php</name>
  <files>
    wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-34-provinces.svg
    wordpress/wp-content/themes/charity-hcm/category.php
  </files>
  <action>
    **Step A: Create vietnam-34-provinces.svg**

    Create `assets/img/vietnam-34-provinces.svg` for Vietnam's 34-province 2025 administrative map.

    Vietnam's Resolution 76/2025/QH15 merged 63 provinces into 34 administrative units. The 34 units are:
    1. Hà Nội (absorbed Hoà Bình, Hà Nam, Vĩnh Phúc) → `VN34-HN`
    2. TP. Hồ Chí Minh (absorbed Bình Dương, Bà Rịa-Vũng Tàu) → `VN34-SG`
    3. Hải Phòng (absorbed Hải Dương) → `VN34-HP`
    4. Đà Nẵng (absorbed Quảng Nam) → `VN34-DN`
    5. Cần Thơ (absorbed Hậu Giang, Sóc Trăng, Bạc Liêu, Cà Mau) → `VN34-CT`
    6. Huế (absorbed Quảng Trị, Quảng Bình) → `VN34-HUE`
    7. Nghệ An (absorbed Hà Tĩnh) → `VN34-NA`
    8. Thanh Hóa (absorbed Ninh Bình, Nam Định, Thái Bình) → `VN34-TH`
    9. Lâm Đồng (absorbed Đắk Nông) → `VN34-LD`
    10. Đắk Lắk (absorbed Gia Lai, Kon Tum) → `VN34-DL`
    11. Khánh Hòa (absorbed Phú Yên, Ninh Thuận, Bình Thuận) → `VN34-KH`
    12. Đồng Nai (absorbed Bình Phước, Tây Ninh, Long An) → `VN34-DN2`
    13. Tiền Giang (absorbed Bến Tre, Trà Vinh, Vĩnh Long) → `VN34-TG`
    14. An Giang (absorbed Đồng Tháp, Kiên Giang) → `VN34-AG`
    15. Quảng Ngãi (absorbed Bình Định) → `VN34-QNG`
    16. Lào Cai (absorbed Yên Bái, Điện Biên, Lai Châu) → `VN34-LC`
    17. Hà Giang (absorbed Tuyên Quang, Bắc Kạn, Cao Bằng) → `VN34-HG`
    18. Thái Nguyên (absorbed Bắc Giang, Lạng Sơn) → `VN34-TN`
    19. Phú Thọ (absorbed Hòa Bình) — wait, Hòa Bình is absorbed into Hà Nội per some sources. Use authoritative grouping.
    
    Note: The exact final list of 34 units was still being finalized in 2025. Use a representative grouping based on publicly available pre-merger planning documents. The SVG is for visual demonstration — accuracy is important but exact legal boundaries are secondary to visual plausibility.
    
    Use `VN34-` prefix for all IDs. Same SVG requirements as Plan 02:
    - `viewBox` based, no fixed dimensions
    - `fill="var(--province-fill, #e8ead6)"` `stroke="#fff"` `stroke-width="0.5"`
    - Wrap all in `<g id="provinces-34">`
    - `<title>Bản đồ 34 tỉnh thành Việt Nam (2025)</title>`
    - Since there are fewer, larger provinces, shapes will be bigger and simpler

    **Step B: Add toggle buttons and second SVG container to category.php**

    In the `student-map-section` from Plan 02, add the toggle buttons BEFORE `.student-map__wrap`:

    ```php
    <!-- Province map toggle -->
    <div class="student-map__toggle" role="group" aria-label="<?php echo esc_attr( charity_t( 'Chọn phân chia tỉnh thành', 'Select province division' ) ); ?>">
        <button class="map-toggle-btn active" data-map="63" type="button">
            <?php echo charity_t( '63 tỉnh thành', '63 Provinces' ); ?>
            <span class="map-toggle-btn__note"><?php echo charity_t( 'Cũ', 'Classic' ); ?></span>
        </button>
        <button class="map-toggle-btn" data-map="34" type="button">
            <?php echo charity_t( '34 tỉnh thành', '34 Provinces' ); ?>
            <span class="map-toggle-btn__note"><?php echo charity_t( 'Mới 2025', 'New 2025' ); ?></span>
        </button>
    </div>
    ```

    Also add the 34-province SVG container AFTER the 63-province container, inside `.student-map__wrap`:

    ```php
    <!-- SVG container: 34-province view (hidden by default) -->
    <div class="student-map__svg-container" id="student-map-34" aria-hidden="true">
        <?php
        $svg_34 = get_template_directory() . '/assets/img/vietnam-34-provinces.svg';
        if ( file_exists( $svg_34 ) ) {
            // phpcs:ignore WordPress.WP.AlternativeFunctions.file_get_contents_file_get_contents
            echo file_get_contents( $svg_34 );
        }
        ?>
    </div>
    ```
  </action>
  <verify>
    <automated>
      test -f wordpress/wp-content/themes/charity-hcm/assets/img/vietnam-34-provinces.svg &amp;&amp; echo "34-SVG exists" &amp;&amp; grep "student-map-34\|map-toggle-btn" wordpress/wp-content/themes/charity-hcm/category.php
    </automated>
  </verify>
  <done>
    34-province SVG exists. category.php contains "student-map-34" and "map-toggle-btn". Both SVG containers are present in the ban-do-vuon-len conditional block.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend student-map.js with toggle logic and add mobile CSS + update wp_localize_script with students_34</name>
  <files>
    wordpress/wp-content/themes/charity-hcm/assets/js/student-map.js
    wordpress/wp-content/themes/charity-hcm/assets/css/main.css
    wordpress/wp-content/themes/charity-hcm/functions.php
  </files>
  <action>
    **Step A: Extend student-map.js with toggle logic**

    REPLACE the full contents of `student-map.js` with the updated version below (preserving all Plan 02 logic, adding toggle):

    ```js
    /* Rise Up Scholarship — student-map.js
     * Interactive choropleth for Bản đồ Vươn Lên (student origin map).
     * Requires: window.vuonlenMap (set via wp_localize_script)
     * Reads:    #student-map-63 and #student-map-34 SVG paths
     */
    (function () {
      'use strict';

      document.addEventListener('DOMContentLoaded', function () {
        var mapData = window.vuonlenMap;
        if (!mapData || !mapData.students) { return; }

        var tooltip    = document.getElementById('student-map-tooltip');
        var containers = {
          '63': document.getElementById('student-map-63'),
          '34': document.getElementById('student-map-34'),
        };
        var toggleBtns = document.querySelectorAll('.map-toggle-btn');
        var activeMap  = '63';

        // Apply fill classes to provinces with student data
        function applyData(container, students) {
          if (!container) { return; }
          container.querySelectorAll('path').forEach(function (path) {
            path.classList.remove('province--low', 'province--mid', 'province--high', 'province--active');
          });
          students.forEach(function (entry) {
            var path = container.querySelector('#' + entry.code);
            if (!path) { return; }
            path.dataset.labelVi = entry.label_vi;
            path.dataset.labelEn = entry.label_en;
            path.dataset.count   = entry.count;
            path.classList.add('province--active');
            if (entry.count >= 8) {
              path.classList.add('province--high');
            } else if (entry.count >= 4) {
              path.classList.add('province--mid');
            } else {
              path.classList.add('province--low');
            }
          });
        }

        // Tooltip
        function showTooltip(e, path) {
          if (!tooltip) { return; }
          var lang  = mapData.lang || 'vi';
          var label = lang === 'en' ? (path.dataset.labelEn || path.dataset.labelVi) : path.dataset.labelVi;
          var unit  = lang === 'en' ? ' students' : ' sinh viên';
          tooltip.textContent = label + ': ' + path.dataset.count + unit;
          tooltip.classList.add('visible');
          positionTooltip(e);
        }

        function hideTooltip() {
          if (tooltip) { tooltip.classList.remove('visible'); }
        }

        function positionTooltip(e) {
          if (!tooltip) { return; }
          var wrap = document.querySelector('.student-map__wrap');
          var rect = wrap ? wrap.getBoundingClientRect() : { left: 0, top: 0 };
          var x    = (e.clientX || (e.touches && e.touches[0] && e.touches[0].clientX) || 0) - rect.left + 12;
          var y    = (e.clientY || (e.touches && e.touches[0] && e.touches[0].clientY) || 0) - rect.top  - 36;
          tooltip.style.left = x + 'px';
          tooltip.style.top  = y + 'px';
        }

        function bindEvents(container) {
          if (!container) { return; }
          container.querySelectorAll('path.province--active').forEach(function (path) {
            path.addEventListener('mouseenter', function (e) { showTooltip(e, path); });
            path.addEventListener('mousemove',  function (e) { positionTooltip(e); });
            path.addEventListener('mouseleave', hideTooltip);
            path.addEventListener('touchstart', function (e) {
              e.preventDefault();
              showTooltip(e, path);
            }, { passive: false });
            path.addEventListener('touchend', function () {
              setTimeout(hideTooltip, 1800);
            });
          });
        }

        // Toggle between 63 and 34 province maps
        function switchMap(toKey) {
          if (toKey === activeMap) { return; }
          hideTooltip();
          // Swap SVG containers
          Object.keys(containers).forEach(function (key) {
            var c = containers[key];
            if (!c) { return; }
            c.classList.toggle('active', key === toKey);
            c.setAttribute('aria-hidden', key !== toKey ? 'true' : 'false');
          });
          // Swap toggle button active state
          toggleBtns.forEach(function (btn) {
            btn.classList.toggle('active', btn.dataset.map === toKey);
          });
          activeMap = toKey;
          // Re-apply data to newly active container (events already bound at init)
        }

        // Init: apply data + bind events for both maps
        var students63 = mapData.students    || [];
        var students34 = mapData.students_34 || [];

        applyData(containers['63'], students63);
        bindEvents(containers['63']);
        applyData(containers['34'], students34);
        bindEvents(containers['34']);

        // Wire toggle buttons
        toggleBtns.forEach(function (btn) {
          btn.addEventListener('click', function () {
            switchMap(btn.dataset.map);
          });
        });
      });
    }());
    ```

    **Step B: Append toggle CSS to main.css**

    In `main.css`, inside the `/* ── Student Map ── */` block added in Plan 02, append BEFORE the closing `/* ── /Student Map ── */` comment:

    ```css
    /* Toggle buttons */
    .student-map__toggle {
      display: flex;
      justify-content: center;
      gap: 0.5rem;
      margin-bottom: var(--space-md);
      flex-wrap: wrap;
    }

    .map-toggle-btn {
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 0.5rem 1.25rem;
      border: 2px solid var(--color-border, #d0d0c8);
      border-radius: var(--radius-md, 8px);
      background: #fff;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--color-text, #333);
      transition: border-color 0.2s ease, background 0.2s ease, color 0.2s ease;
      line-height: 1.2;
    }

    .map-toggle-btn__note {
      font-size: 0.7rem;
      font-weight: 400;
      color: var(--color-text-muted, #999);
      margin-top: 2px;
    }

    .map-toggle-btn.active,
    .map-toggle-btn:hover {
      border-color: var(--color-red, #c0392b);
      background: var(--color-red, #c0392b);
      color: #fff;
    }

    .map-toggle-btn.active .map-toggle-btn__note,
    .map-toggle-btn:hover .map-toggle-btn__note {
      color: rgba(255, 255, 255, 0.75);
    }

    @media (max-width: 480px) {
      .map-toggle-btn {
        padding: 0.4rem 0.9rem;
        font-size: 0.8rem;
      }
    }
    ```

    **Step C: Add students_34 data to wp_localize_script in functions.php**

    Find the `wp_localize_script( 'charity-student-map', 'vuonlenMap', [...] )` call added in Plan 02 and add `'students_34'` to the data array:

    ```php
    $student_data_34 = [
        [ 'code' => 'VN34-SG',  'label_vi' => 'TP. Hồ Chí Minh (mở rộng)', 'label_en' => 'Ho Chi Minh City (expanded)', 'count' => 13 ],
        [ 'code' => 'VN34-HN',  'label_vi' => 'Hà Nội (mở rộng)',           'label_en' => 'Hanoi (expanded)',            'count' => 8  ],
        [ 'code' => 'VN34-DN',  'label_vi' => 'Đà Nẵng (mở rộng)',          'label_en' => 'Da Nang (expanded)',          'count' => 5  ],
        [ 'code' => 'VN34-CT',  'label_vi' => 'Cần Thơ (mở rộng)',          'label_en' => 'Can Tho (expanded)',          'count' => 4  ],
        [ 'code' => 'VN34-HP',  'label_vi' => 'Hải Phòng (mở rộng)',        'label_en' => 'Hai Phong (expanded)',        'count' => 3  ],
        [ 'code' => 'VN34-HUE', 'label_vi' => 'Huế (mở rộng)',              'label_en' => 'Hue (expanded)',              'count' => 3  ],
        [ 'code' => 'VN34-NA',  'label_vi' => 'Nghệ An (mở rộng)',          'label_en' => 'Nghe An (expanded)',          'count' => 2  ],
        [ 'code' => 'VN34-KH',  'label_vi' => 'Khánh Hòa (mở rộng)',       'label_en' => 'Khanh Hoa (expanded)',        'count' => 1  ],
    ];

    wp_localize_script( 'charity-student-map', 'vuonlenMap', [
        'lang'        => function_exists( 'charity_get_lang' ) ? charity_get_lang() : 'vi',
        'students'    => $student_data,    // 63-province data (from Plan 02)
        'students_34' => $student_data_34, // 34-province data (new)
    ] );
    ```

    This REPLACES the existing `wp_localize_script` call (not a second call — two `wp_localize_script` calls with the same handle merge, but it's cleaner to have one).
  </action>
  <verify>
    <automated>
      grep "students_34\|switchMap\|map-toggle-btn" wordpress/wp-content/themes/charity-hcm/assets/js/student-map.js | head -5 &amp;&amp; grep "students_34" wordpress/wp-content/themes/charity-hcm/functions.php &amp;&amp; php -l wordpress/wp-content/themes/charity-hcm/functions.php
    </automated>
  </verify>
  <done>
    student-map.js contains "switchMap" and toggle logic. functions.php contains "students_34" and passes PHP syntax check. CSS contains ".map-toggle-btn".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Full Phase 6 implementation:
    - Contact page address and phone updated (Plan 01)
    - Interactive 63-province choropleth map on ban-do-vuon-len category (Plan 02)
    - 34-province toggle + mobile responsive CSS (Plan 03)
  </what-built>
  <how-to-verify>
    1. Visit http://127.0.0.1:8080/lien-he/
       - Address shows: 43D/46 Hồ Văn Huê, P. Đức Nhuận, TP. HCM
       - Phone shows: 084.3214.142

    2. Visit http://127.0.0.1:8080/?cat={ban-do-vuon-len-id} (find the category ID in WP admin → Posts → Categories)
       - Interactive SVG map is visible (not static image)
       - Two toggle buttons appear: "63 tỉnh thành / Cũ" and "34 tỉnh thành / Mới 2025"
       - Highlighted provinces (HCM, Hanoi, etc.) have red fill
       - Hover over a highlighted province → tooltip shows name + count

    3. Click "34 tỉnh thành" toggle
       - Map swaps to 34-province view
       - Highlighted provinces update (fewer, larger regions)
       - Toggle button shows active state (red background)

    4. Resize browser to 375px width
       - Map is centered and legible
       - Toggle buttons are properly stacked/sized

    5. Toggle language to EN
       - Tooltip shows English province name + "students"
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS toggle buttons → DOM swap | User interaction; JS only swaps CSS classes, no server calls |
| PHP → HTML (34-province SVG inline) | Same pattern as Plan 02; SVG is a local theme asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Toggle button data-map attribute | mitigate | JS only accepts '63' or '34' as valid keys via `containers[key]` lookup — unknown keys silently no-op (key not in containers object = undefined = skipped) |
| T-06-03-02 | XSS | Inline SVG (34-province) | mitigate | Same as T-06-02-02 — local theme asset, not user-supplied. SVG must not contain `<script>` tags. |
| T-06-03-03 | Information Disclosure | students_34 sample data | accept | Intentionally public sample data — no PII |
</threat_model>

<verification>
End-to-end Phase 6 checklist:
1. [ ] `/lien-he/` shows correct address (P. Đức Nhuận) and phone (084.3214.142)
2. [ ] `ban-do-vuon-len` category page shows interactive SVG choropleth (no static img)
3. [ ] 12 provinces have fill color (3 tiers: low/mid/high)
4. [ ] Hover tooltip shows province name + student count
5. [ ] Toggle switches between 63-province and 34-province SVG views
6. [ ] Active toggle button is visually highlighted (red)
7. [ ] Mobile (375px): map and toggle buttons are usable
8. [ ] EN mode: tooltip shows English label
9. [ ] student-map.js does NOT load on other pages (Network tab check)
10. [ ] No PHP errors: `php -l functions.php` and `php -l category.php` pass
11. [ ] All new strings use `charity_t()` bilingual helper
</verification>

<success_criteria>
34-province toggle works. Both maps render with choropleth data. Mobile layout is clean. All Phase 6 ROADMAP success criteria are met: address updated, interactive map with sample data displayed, toggle functional, mobile-responsive, bilingual strings in charity_t().
</success_criteria>

<output>
After completion, create `.planning/phases/06-cap-nhat-section-lien-he-va-ban-do-dong-du-voi-ban-do-tuong-/06-03-SUMMARY.md`
</output>
