---
phase: 01-css-cleanup-foundation-polish
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - wordpress/wp-content/themes/charity-hcm/assets/css/main.css
autonomous: true
requirements: [UI-01, UI-03]

must_haves:
  truths:
    - main.css parses without unclosed braces
    - .cp-group-card and .cp-group-item hover interactions work visually
    - All Phase 1 success criteria are satisfied
  artifacts:
    - .planning/phases/01-css-cleanup-foundation-polish/01-02-SUMMARY.md
  key_links:
    - 01-01-PLAN.md → 01-02-PLAN.md (verification of plan 01 changes)
---

<objective>
Verify Phase 1 CSS changes are correct and produce a SUMMARY.

Purpose: Confirm all success criteria from Phase 1 are met, fix any issues found, and commit.
Output: Clean main.css passing all checks + 01-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/phases/01-css-cleanup-foundation-polish/01-01-PLAN.md
@wordpress/wp-content/themes/charity-hcm/assets/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all Phase 1 success criteria checks</name>
  <files>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</files>
  <read_first>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</read_first>
  <action>
    Run all verification checks:
    1. grep -c ".btn--primary:hover" main.css → must be 1
    2. grep ".btn--red:hover" main.css → must find a result
    3. grep -c "Phase 2: UI Polish" main.css → must be 0
    4. grep "transition.*transform" main.css → must show results for cp-group-card section
    5. Count braces: python3 -c "c=open('main.css').read(); print('open:',c.count('{'),'close:',c.count('}'))" → counts should be equal or off by 1 (due to :root pseudo-selector nesting in some browsers)
    
    Fix any failing check before proceeding.
  </action>
  <verify>All 5 checks pass</verify>
  <acceptance_criteria>
    - .btn--primary:hover count = 1
    - .btn--red:hover exists
    - "Phase 2: UI Polish" count = 0
    - cp-group-card has transition property in its main rule
    - Brace count is balanced
  </acceptance_criteria>
  <done>All 5 verification checks pass</done>
</task>

<task type="auto">
  <name>Task 2: Write 01-02-SUMMARY.md</name>
  <files>.planning/phases/01-css-cleanup-foundation-polish/01-02-SUMMARY.md</files>
  <action>
    Create .planning/phases/01-css-cleanup-foundation-polish/01-02-SUMMARY.md with:
    - What was fixed (4 specific changes made)
    - Verification results (all checks that passed)
    - Line count before and after (grep -c "" main.css before was 2309)
    - No regressions found note
  </action>
  <verify>File exists</verify>
  <acceptance_criteria>
    - File exists at correct path
    - Contains actual line count after cleanup
  </acceptance_criteria>
  <done>.planning/phases/01-css-cleanup-foundation-polish/01-02-SUMMARY.md exists</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All success criteria checks pass
- [ ] 01-02-SUMMARY.md written
</verification>

<success_criteria>
- All verification checks pass
- Summary file written with accurate results
</success_criteria>
