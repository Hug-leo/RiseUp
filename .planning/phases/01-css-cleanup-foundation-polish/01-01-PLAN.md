---
phase: 01-css-cleanup-foundation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wordpress/wp-content/themes/charity-hcm/assets/css/main.css
autonomous: true
requirements: [UI-01, UI-03]

must_haves:
  truths:
    - .btn--primary:hover appears exactly once in main.css
    - .btn--red:hover exists in main.css
    - .cp-group-card has transition and :hover in its base section (no separate Phase 2 block)
    - .cp-group-item has transition and :hover in its base section (no separate Phase 2 block)
    - "Phase 2: UI Polish" comment no longer exists in main.css
    - Only one @media (max-width: 768px) block with cp-* rules (not two separate blocks)
  artifacts:
    - wordpress/wp-content/themes/charity-hcm/assets/css/main.css
  key_links: []
---

<objective>
Fix CSS bugs and consolidate orphan rule fragments in main.css.

Purpose: Lay a clean CSS foundation before adding new section styles in Phase 2. Fix a real selector bug (.btn--red has no hover state), merge hover states added in a "Phase 2" orphan block into their proper base rules, and consolidate duplicate @media 768px blocks.
Output: A clean main.css with no duplicate rule blocks, no orphan comment sections, and .btn--red with a proper hover state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@wordpress/wp-content/themes/charity-hcm/assets/css/main.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix duplicate .btn--primary:hover (rename second occurrence to .btn--red:hover)</name>
  <files>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</files>
  <read_first>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</read_first>
  <action>
    In main.css, around line 395-403, there is:
    
      .btn--red {
        background: var(--primary);
        color: #fff;
      }
      .btn--primary:hover {
        background: var(--primary-dark);
        transform: translateY(-1px);
      }
    
    The second `.btn--primary:hover` is clearly intended to be `.btn--red:hover` (it immediately follows .btn--red and overrides .btn--primary:hover which is already defined above at line 370).
    
    Change `.btn--primary:hover` at ~line 399 to `.btn--red:hover`. Do NOT touch the first `.btn--primary:hover` at ~line 370.
  </action>
  <verify>grep -c "\.btn--primary:hover" wordpress/wp-content/themes/charity-hcm/assets/css/main.css</verify>
  <acceptance_criteria>
    - Output of grep is "1" (only one occurrence of .btn--primary:hover)
    - grep finds ".btn--red:hover" in main.css
  </acceptance_criteria>
  <done>grep -c ".btn--primary:hover" returns 1 AND grep finds ".btn--red:hover"</done>
</task>

<task type="auto">
  <name>Task 2: Merge .cp-group-card hover states into base rule and remove Phase 2 orphan</name>
  <files>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</files>
  <read_first>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</read_first>
  <action>
    At ~line 1963, the base .cp-group-card rule is:
    
      .cp-group-card {
        background: #fff;
        border: 1px solid #d7e7ff;
        border-top: 6px solid var(--primary);
        border-radius: 14px;
        padding: 18px;
        box-shadow: 0 10px 28px rgba(14, 68, 146, 0.08);
      }
    
    At ~line 2283 (in the "Phase 2: UI Polish" orphan section), there are augmenting rules:
    
      .cp-group-card {
        transition: transform .3s, box-shadow .3s;
      }
      .cp-group-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 32px rgba(14, 68, 146, 0.14);
      }
    
    Steps:
    1. Add `transition: transform .3s, box-shadow .3s;` to the base `.cp-group-card` rule (inside its braces).
    2. Add a `.cp-group-card:hover { ... }` rule immediately AFTER the base `.cp-group-card { ... }` closing brace.
    3. After merging, the "Phase 2: UI Polish" duplicate .cp-group-card block (at end of file) will be removed in Task 4.
    
    The merged base rule should look like:
    
      .cp-group-card {
        background: #fff;
        border: 1px solid #d7e7ff;
        border-top: 6px solid var(--primary);
        border-radius: 14px;
        padding: 18px;
        box-shadow: 0 10px 28px rgba(14, 68, 146, 0.08);
        transition: transform .3s, box-shadow .3s;
      }
      .cp-group-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 32px rgba(14, 68, 146, 0.14);
      }
  </action>
  <verify>grep -A8 "\.cp-group-card {" wordpress/wp-content/themes/charity-hcm/assets/css/main.css | head -15</verify>
  <acceptance_criteria>
    - The base .cp-group-card rule contains "transition: transform .3s"
    - A .cp-group-card:hover rule exists adjacent to the base rule (not only at end of file)
  </acceptance_criteria>
  <done>Base .cp-group-card has transition property and is followed by .cp-group-card:hover rule</done>
</task>

<task type="auto">
  <name>Task 3: Merge .cp-group-item hover states into base rule</name>
  <files>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</files>
  <read_first>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</read_first>
  <action>
    At ~line 1994, the base .cp-group-item rule is:
    
      .cp-group-item {
        border: 1px solid #e8f1ff;
        border-radius: 10px;
        padding: 10px 12px;
        background: #fcfeff;
      }
    
    At ~line 2291 (in the "Phase 2: UI Polish" orphan section), there are augmenting rules:
    
      .cp-group-item {
        transition: transform var(--transition), box-shadow var(--transition), border-color var(--transition);
      }
      .cp-group-item:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(14, 68, 146, 0.10);
        border-color: var(--primary-light);
      }
    
    Steps:
    1. Add `transition: transform var(--transition), box-shadow var(--transition), border-color var(--transition);` to the base `.cp-group-item` rule.
    2. Add a `.cp-group-item:hover { ... }` rule immediately AFTER the base `.cp-group-item { ... }` closing brace.
    
    The merged base rule should look like:
    
      .cp-group-item {
        border: 1px solid #e8f1ff;
        border-radius: 10px;
        padding: 10px 12px;
        background: #fcfeff;
        transition: transform var(--transition), box-shadow var(--transition), border-color var(--transition);
      }
      .cp-group-item:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(14, 68, 146, 0.10);
        border-color: var(--primary-light);
      }
  </action>
  <verify>grep -A8 "\.cp-group-item {" wordpress/wp-content/themes/charity-hcm/assets/css/main.css | head -15</verify>
  <acceptance_criteria>
    - The base .cp-group-item rule contains "transition: transform var(--transition)"
    - A .cp-group-item:hover rule exists adjacent to the base rule
  </acceptance_criteria>
  <done>Base .cp-group-item has transition property and is followed by .cp-group-item:hover rule</done>
</task>

<task type="auto">
  <name>Task 4: Remove "Phase 2: UI Polish" orphan comment block and merge .cp-hero h1 into cp-hero section</name>
  <files>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</files>
  <read_first>wordpress/wp-content/themes/charity-hcm/assets/css/main.css</read_first>
  <action>
    At the end of main.css is the "Phase 2: UI Polish" comment block:
    
      /* ── Phase 2: UI Polish ─────────────────────────────────────────────────── */
      .cp-group-card {
        transition: transform .3s, box-shadow .3s;
      }
      .cp-group-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 12px 32px rgba(14, 68, 146, 0.14);
      }
      
      .cp-group-item {
        transition: transform var(--transition), box-shadow var(--transition), border-color var(--transition);
      }
      .cp-group-item:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 12px rgba(14, 68, 146, 0.10);
        border-color: var(--primary-light);
      }
      
      .cp-hero h1 {
        text-shadow: 0 2px 16px rgba(0, 20, 60, 0.25);
      }
      
      @media (max-width: 768px) {
        .cp-hero__logo {
          width: 84px;
          height: 84px;
        }
      }
    
    Steps:
    1. Before deleting, first add `text-shadow: 0 2px 16px rgba(0, 20, 60, 0.25);` to the `.cp-hero h1` rule in the CONGPHAT homepage refresh section (find the existing .cp-hero h1 rule there, or if it doesn't exist, add it after the .cp-hero__title rule).
    2. The `.cp-hero__logo { width: 84px; height: 84px; }` @media 768px override: add this rule to the existing `@media (max-width: 768px)` block in the CONGPHAT section (which already has `.cp-hero { padding-top: 68px; }` and `.cp-hero__logo { width: 102px; }` — update that to `width: 84px; height: 84px;` since the Phase 2 block overrides it anyway).
    3. Remove the ENTIRE "Phase 2: UI Polish" block from the end of the file (including the comment, all rules, and the @media block).
    
    Note: The .cp-group-card and .cp-group-item hover rules were already merged into their base rules in Tasks 2 and 3, so removing them here is safe.
  </action>
  <verify>grep -c "Phase 2: UI Polish" wordpress/wp-content/themes/charity-hcm/assets/css/main.css</verify>
  <acceptance_criteria>
    - grep returns "0" (no "Phase 2: UI Polish" comment in file)
    - .cp-hero h1 has text-shadow property somewhere in the file
  </acceptance_criteria>
  <done>No "Phase 2: UI Polish" comment exists and .cp-hero h1 has text-shadow</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] grep -c "\.btn--primary:hover" main.css returns 1
- [ ] grep "\.btn--red:hover" main.css returns a result
- [ ] grep -c "Phase 2: UI Polish" main.css returns 0
- [ ] grep "transition.*transform" main.css | grep "cp-group-card\|cp-group-item" — returns results within base rule sections
- [ ] File has no syntax errors (no unclosed braces) — check by counting { vs } in the file
</verification>

<success_criteria>
- All 4 tasks completed
- .btn--primary:hover appears exactly once
- .btn--red:hover exists
- .cp-group-card and .cp-group-item each have transition + hover adjacent to their base rules
- "Phase 2: UI Polish" comment section removed
- .cp-hero h1 has text-shadow property
</success_criteria>
